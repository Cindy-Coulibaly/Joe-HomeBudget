<Window x:Class="JoeWpfHomeBudget.TestDoubleClick"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JoeWpfHomeBudget"
        mc:Ignorable="d"
        Title="TestDoubleClick" Height="450" Width="800">
    <Grid>
        <ListBox x:Name="BudgetItems" ItemsSource="{Binding}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <ListBoxItem MouseDoubleClick="TextBlock_MouseDoubleClick">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Date, StringFormat='MM/dd/yyyy'}" Margin="5"/>
                            <TextBlock Text="{Binding Category}" Margin="5"/>
                            <TextBlock Text="{Binding Amount}" Margin="5"/>
                            <TextBlock Text="{Binding Description}" Margin="5"/>
                        </StackPanel>
                    </ListBoxItem>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>